# Nexus-Stack for Education - Environment Variables (Secrets)
# ============================================================
# These are SHARED across all user deployments.
# Keep this file secure - never commit to git!
#
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#   nano .env

# =============================================================================
# Required: Hetzner Cloud
# =============================================================================
# 1. Create a project at: https://console.hetzner.cloud
# 2. Go to: Security → API Tokens → Generate API Token
# 3. Select "Read & Write" permissions
export TF_VAR_hcloud_token=""

# =============================================================================
# Required: Cloudflare
# =============================================================================
# API Token with permissions: Zone:Read, DNS:Edit, Cloudflare Tunnel:Edit, Access:Edit
# Get from: https://dash.cloudflare.com/profile/api-tokens
export TF_VAR_cloudflare_api_token=""

# Account ID - found on any zone overview page (right sidebar)
export TF_VAR_cloudflare_account_id=""

# =============================================================================
# Optional: Docker Hub (for higher pull rate limits)
# =============================================================================
# Without this: 100 pulls/6h per IP
# With this: 200 pulls/6h
# export TF_VAR_dockerhub_username=""
# export TF_VAR_dockerhub_token=""

# =============================================================================
# Optional: Resend (for email notifications)
# =============================================================================
# Get your API key from: https://resend.com/api-keys
# export TF_VAR_resend_api_key=""
